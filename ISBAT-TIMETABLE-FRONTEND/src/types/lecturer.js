// Type definitions for Lecturer (converted from TypeScript)
// These are kept as comments for reference but not enforced at runtime

/**
 * @typedef {Object} Lecturer
 * @property {string} id
 * @property {string} name
 * @property {string} faculty
 * @property {"Professor" | "Lecturer" | "Assistant"} role
 * @property {string[]} specializations - Array of course unit IDs
 * @property {number} maxHours
 * @property {Record<string, string[]>} [availability] - e.g., { Monday: ["9-11","2-4"] }
 */

// Export empty object to maintain module structure
export {};

