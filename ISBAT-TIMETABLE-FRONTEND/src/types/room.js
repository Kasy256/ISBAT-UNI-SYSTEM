// Type definitions for Room (converted from TypeScript)
// These are kept as comments for reference but not enforced at runtime

/**
 * @typedef {Object} Room
 * @property {string} id
 * @property {string} roomId
 * @property {"Lecture Hall" | "Lab" | "Classroom"} type
 * @property {number} capacity
 * @property {string} [specialization]
 */

// Export empty object to maintain module structure
export {};

